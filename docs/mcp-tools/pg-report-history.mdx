---
title: pg_report_history
description: View past Postgres analysis reports and compare trends
---

View past Postgres analysis reports and compare trends. Shows changes in table sizes, dead tuples, recommendations gained/resolved. Use this to track database health over time.

## Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `count` | number | No | `5` | Number of past reports to load (1-50) |
| `diff` | boolean | No | `true` | Compare latest two reports and show changes |

## Example Request

### Default (Last 5 Reports with Diff)

```json
{}
```

### Last 20 Reports Without Diff

```json
{
  "count": 20,
  "diff": false
}
```

### Compare Recent Reports

```json
{
  "count": 2,
  "diff": true
}
```

## Notes

- This tool is **read-only** and **not destructive** — it only reads saved report files.
- Requires the `DATABASE_URL` environment variable to be set (direct `pg` connection, independent of Supabase).
- Reports must have been previously saved using [`pg_analyze`](/docs/mcp-tools/pg-analyze) with `save: true`.
- Reports are loaded from the report directory (`PG_REPORT_DIR`, default `./reports/pg-analysis`).
- When `diff` is `true`, the comparison highlights:
  - **Table size changes**: growth or shrinkage since the previous report
  - **Dead tuple changes**: increases or decreases in dead tuples
  - **Recommendations gained**: new issues detected since the last report
  - **Recommendations resolved**: issues that are no longer present

## Related Tools

- [`pg_analyze`](/docs/mcp-tools/pg-analyze) - Run a new analysis (use `save: true` to add to history)
- [`pg_recommendations`](/docs/mcp-tools/pg-recommendations) - Get current optimization recommendations
