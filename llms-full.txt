# Textrawl - Complete Documentation

> Personal knowledge server — your documents, searchable and AI-ready

## Overview

Textrawl is a personal knowledge server with persistent memory, searchable documents, and proactive insights. Access it through the web dashboard, MCP for AI assistants, or the REST API. Backed by Neon PostgreSQL with vector embeddings.

## Quick Start

```bash
git clone https://github.com/jeffgreendesign/textrawl.git
cd textrawl
pnpm setup
pnpm dev
pnpm inspector  # Test at http://localhost:5173
```

## MCP Server Configuration

- Protocol: Model Context Protocol (MCP)
- Transport: StreamableHTTPServerTransport (stateless HTTP)
- Endpoint: POST /mcp
- Authentication: Bearer token (API_BEARER_TOKEN)
- Rate Limits: API 100/min, Upload 10/min
- Response Format: Compact by default (COMPACT_RESPONSES=true)

### Compact Response Format

Memory tools return token-efficient responses by default, reducing context usage by 40-60%. Set `COMPACT_RESPONSES=false` for verbose human-readable responses.

**Key mappings (compact → verbose):**
| Compact | Verbose | Used In |
|---------|---------|---------|
| `n` | `name` or `count` | All tools (context-dependent) |
| `t` | `type` | Entity type |
| `o` | `observations` | query_memory (entity mode) |
| `m` | `memories` | query_memory (search mode) |
| `c` | `content` | Memory content |
| `s` | `score` | Search relevance |
| `r` | `relations` | Entity relations |
| `ok` | `success` | Operation status |
| `dup` | `duplicate` | Dedupe detection |
| `ent`/`obs`/`rel` | `totalEntities`/etc. | get_stats (memory scope) |

### Claude Desktop Configuration

```json
{
  "mcpServers": {
    "textrawl": {
      "url": "http://localhost:3000/mcp"
    }
  }
}
```

## MCP Tools Reference

### Structured Output

Several read-only tools define `outputSchema` and return `structuredContent` alongside the text `content` response. The `structuredContent` object always uses canonical verbose keys regardless of `COMPACT_RESPONSES`. Tools with `outputSchema`: `search`, `get_document`, `list_documents`, `query_memory`, `query_conversations`, `get_stats`, `health_check`.

### search

Hybrid semantic + full-text search with optional cross-source memory and conversation results. Replaces the previous `search_knowledge` and `search_with_context` tools.

Parameters:
- query (string, REQUIRED): Natural language search query (1-10000 chars)
- limit (number): Max results 1-50, default 10
- fullTextWeight (number): Keyword weight 0-2, default 1.0
- semanticWeight (number): Semantic weight 0-2, default 1.0
- tags (string[]): Filter to docs with ALL tags
- sourceType (enum): 'note' | 'file' | 'url'
- contentType (enum): Content type filter
- minScore (number): Minimum relevance 0-1
- includeMemories (boolean): Include memory results, default false
- includeConversations (boolean): Include conversation results, default false
- memoryWeight (number): Weight for memory results 0-2
- conversationWeight (number): Weight for conversation results 0-2

Example (document search only):
```json
{
  "query": "quarterly planning",
  "limit": 5,
  "semanticWeight": 1.5
}
```

Example (cross-source search):
```json
{
  "query": "project architecture decisions",
  "includeMemories": true,
  "includeConversations": true,
  "memoryWeight": 1.0,
  "conversationWeight": 0.5
}
```

### get_document

Retrieve full document by ID.

Parameters:
- documentId (UUID, REQUIRED): The document UUID
- includeChunks (boolean): Include chunks, default false

### list_documents

List documents with pagination.

Parameters:
- limit (number): 1-100, default 20
- offset (number): Pagination offset
- sourceType (enum): 'note' | 'file' | 'url'
- tags (string[]): Filter by tags

### update_document

Update document metadata.

Parameters:
- documentId (UUID, REQUIRED): Document to update
- title (string): New title
- tags (string[]): New tags (replaces existing)

Note: MUST provide title or tags.

### add_note

Create markdown notes with automatic embedding.

Parameters:
- title (string, REQUIRED): Note title (max 500 chars)
- content (string, REQUIRED): Markdown content (max 1MB)
- tags (string[]): Tags for organization

### capture

Workflow tool exposed on the compact `normal` surface (MCP_TOOLSET=normal). Saves content to the knowledge base, consolidating add_note + save_url behind one content-acquisition workflow. Reuses the same chunk/embed/persist pipeline as the legacy tools.

Parameters:
- kind (enum, REQUIRED): "note" or "url"
- title (string): note title (required for kind=note) or override for kind=url
- content (string): markdown body (required for kind=note, max 1MB)
- url (string): web page URL (required for kind=url)
- tags (string[]): tags for organization
- audience (enum): private_jeff | family_shared | public_safe — stored in metadata
- extractMemories (boolean): default false; requires ENABLE_MEMORY_EXTRACTION

### remember

Workflow tool exposed on the compact `normal` surface. Writes structured knowledge to the memory graph, consolidating remember_fact + build_knowledge + relate_entities. At least one of facts/relations is required; per-item failures are reported as partial success.

Parameters:
- facts (array, max 50): [{ entityName, entityType, observation, source? }] — a single fact is a one-element array
- relations (array, max 50): [{ fromEntity, relation, toEntity, fromEntityType?, toEntityType? }]

## Memory Tools (Persistent Memory)

Enable with ENABLE_MEMORY=true (default). Requires setup-db-memory.sql (OpenAI) or setup-db-memory-ollama.sql (Ollama).

### remember_fact

Store facts about entities (people, projects, concepts) with automatic semantic embedding.

Parameters:
- entityName (string, REQUIRED): Name of entity (e.g., "Ada", "Project Alpha")
- entityType (enum, REQUIRED): person | concept | project | preference | fact | location | organization
- observation (string, REQUIRED): Single atomic fact to remember (max 2000 chars)
- source (enum): conversation | note | document | manual, default 'conversation'
- validUntil (string): ISO date for expiring facts (e.g., "2026-12-31")

Example:
```json
{
  "entityName": "Ada",
  "entityType": "person",
  "observation": "prefers dark mode in IDEs",
  "source": "conversation"
}
```

### build_knowledge

Store multiple facts and relations in a single batch call. Prefer this over separate remember_fact and relate_entities calls.

Parameters:
- facts (array, max 50): Array of facts to store
  - entityName (string, REQUIRED): Entity name (1-200 chars)
  - entityType (enum, REQUIRED): person | concept | project | preference | fact | location | organization
  - observation (string, REQUIRED): Fact to remember (1-2000 chars)
  - source (enum): conversation | note | document | manual, default 'conversation'
- relations (array, max 50): Array of relations to create
  - fromEntity (string, REQUIRED): Source entity name
  - relation (string, REQUIRED): Relationship type (e.g., "works on", "manages")
  - toEntity (string, REQUIRED): Target entity name
  - fromEntityType (enum): Type of source entity
  - toEntityType (enum): Type of target entity

Example:
```json
{
  "facts": [
    {
      "entityName": "Ada",
      "entityType": "person",
      "observation": "leads the backend team"
    },
    {
      "entityName": "Project Alpha",
      "entityType": "project",
      "observation": "uses PostgreSQL for the database layer"
    }
  ],
  "relations": [
    {
      "fromEntity": "Ada",
      "relation": "works on",
      "toEntity": "Project Alpha"
    }
  ]
}
```

### query_memory

Unified memory query tool that replaces `recall_memories`, `get_entity_context`, and `list_entities`. Dispatches based on mode.

Parameters:
- mode (enum, REQUIRED): 'search' | 'entity' | 'list'
- query (string): What to search for, required when mode is 'search' (1-1000 chars)
- entityTypes (enum[]): Filter by entity types (search and list modes)
- searchMode (enum): hybrid | semantic, default 'hybrid' (search mode only)
- entityName (string): Entity to look up, required when mode is 'entity'
- includeRelated (boolean): Include relations in entity mode, default true
- maxObs (number): Max observations per entity
- limit (number): Max results (search: 1-50, default 10; list: 1-100, default 50)
- offset (number): Pagination offset (list mode)

Example (search mode - replaces recall_memories):
```json
{
  "mode": "search",
  "query": "user preferences for development",
  "entityTypes": ["person", "preference"],
  "limit": 10
}
```

Example (entity mode - replaces get_entity_context):
```json
{
  "mode": "entity",
  "entityName": "Ada",
  "includeRelated": true
}
```

Example (list mode - replaces list_entities):
```json
{
  "mode": "list",
  "entityTypes": ["person"],
  "limit": 50,
  "offset": 0
}
```

### relate_entities

Create directed relationships between entities.

Parameters:
- fromEntity (string, REQUIRED): Source entity name
- relation (string, REQUIRED): Relation type (works_at, knows, prefers, created, part_of, related_to)
- toEntity (string, REQUIRED): Target entity name
- fromEntityType (enum): Type of source entity (auto-inferred if exists)
- toEntityType (enum): Type of target entity (auto-inferred if exists)

Example:
```json
{
  "fromEntity": "Ada",
  "relation": "works_at",
  "toEntity": "Acme Corp"
}
```

### forget_entity

Delete an entity and all its associated memories and relations.

Parameters:
- entityName (string, REQUIRED): Name of entity to delete
- confirm (boolean, REQUIRED): Must be true to confirm deletion

### extract_memories

Extract entities and facts from text using LLM analysis. Requires ENABLE_MEMORY_EXTRACTION=true and ANTHROPIC_API_KEY.

Parameters:
- text (string, REQUIRED): Text to extract entities and facts from (10-100000 chars)
- source (enum): conversation | note | document | manual, default 'document'
- storeResults (boolean): Store extracted memories in database, default false (preview only)

## Conversation Tools (Conversation Memory)

Enable with ENABLE_CONVERSATIONS=true (default). Requires setup-db-conversation.sql (OpenAI) or setup-db-conversation-ollama.sql (Ollama).

### save_conversation_context

Save conversation summary and turns for later recall.

Parameters:
- summary (string, REQUIRED): Summary of the conversation context (1-10000 chars)
- sessionKey (string): Optional key to identify this conversation (1-200 chars)
- title (string): Conversation title (max 500 chars)
- recentTurns (array): Recent turns to save (max 50), each with role (user|assistant|system) and content
- embedTurns (boolean): Generate embeddings for individual turns, default false

### query_conversations

Unified conversation query tool that replaces `recall_conversation`, `get_conversation`, and `list_conversations`. Dispatches based on mode.

Parameters:
- mode (enum, REQUIRED): 'search' | 'get' | 'list'
- query (string): What to search for, required when mode is 'search' (1-1000 chars)
- searchMode (enum): summary | turns | both, default 'summary' (search mode only)
- includeTranscript (boolean): Include recent turns from matches, default false (search mode)
- maxTurnsPerConversation (number): Max turns per conversation in search results, default 10
- sessionId (string): Session ID to retrieve, for get mode
- sessionKey (string): Session key to retrieve, for get mode
- maxTurns (number): Maximum turns to include (get mode), default 50
- limit (number): Max results (search: 1-20, default 5; list: 1-50, default 20)
- offset (number): Pagination offset (list mode)

Example (search mode - replaces recall_conversation):
```json
{
  "mode": "search",
  "query": "deployment configuration discussion",
  "searchMode": "both",
  "includeTranscript": true
}
```

Example (get mode - replaces get_conversation):
```json
{
  "mode": "get",
  "sessionKey": "project-kickoff-2026",
  "maxTurns": 20
}
```

Example (list mode - replaces list_conversations):
```json
{
  "mode": "list",
  "limit": 20,
  "offset": 0
}
```

### delete_conversation

Delete a conversation session.

Parameters:
- sessionId (string): Session ID to delete
- sessionKey (string): Session key to delete
- confirm (boolean, REQUIRED): Must be true to confirm deletion

## Stats

### get_stats

Get statistics across the knowledge base. Replaces `knowledge_stats`, `memory_stats`, `conversation_stats`, and `insight_stats`. Dispatches based on scope.

Parameters:
- scope (enum): 'all' | 'knowledge' | 'memory' | 'conversations' | 'insights', default 'all'

Example:
```json
{
  "scope": "all"
}
```

### health_check

Check the health of all Textrawl subsystems. Returns pass/fail per component with an overall status (healthy, degraded, unhealthy). Use as the first diagnostic step when something seems broken.

Parameters:
- verbose (boolean): Include latency measurements and row counts, default false

Example:
```json
{
  "verbose": true
}
```

## Insight Tools (Proactive Discovery)

Enable with ENABLE_INSIGHTS=true (default).

### get_insights

Get proactive insights discovered from your knowledge base. Shows cross-source connections, recurring themes, entity bridges, and outliers.

Parameters:
- status (enum): new | seen | dismissed, default 'new'
- insightType (enum): cross_source | theme_cluster | entity_bridge | temporal_pattern | outlier
- query (string): Semantic search query to find relevant insights
- limit (number): Max insights 1-50, default 5

### discover_connections

Trigger an insight scan to discover connections and patterns across the knowledge base.

Parameters:
- fullScan (boolean): Scan all content (not just recent), default false
- maxChunks (number): Max chunks to analyze 10-1000, default 200

### dismiss_insight

Dismiss an insight so it no longer appears in new/seen results.

Parameters:
- insightId (string, REQUIRED): The insight ID to dismiss

## Unified Tools

### ask

Unified RAG search across all knowledge sources (documents, memory, conversations, insights). Returns search results with citations and relevance scores.

Parameters:
- query (string, REQUIRED): Natural language search query
- scope (enum): 'auto' | 'documents' | 'memory' | 'conversations' | 'insights', default 'auto'
- limit (number): Max results 1-50, default 10

Example:
```json
{
  "query": "project architecture decisions",
  "scope": "auto",
  "limit": 10
}
```

### daily_briefing

Generate a daily briefing with recent additions, new insights, and resurfaced "on this day" knowledge. No required parameters.

Example:
```json
{}
```

### save_url

Fetch a URL, convert HTML to markdown, extract metadata (title, author, date), and save as a document.

Parameters:
- url (string, REQUIRED): URL to fetch and save
- tags (string[]): Tags for organization
- extract_memories (boolean): Extract memories from content

Example:
```json
{
  "url": "https://example.com/article",
  "tags": ["research", "ai"],
  "extract_memories": true
}
```

### timeline

Browse knowledge chronologically within a date range, optionally filtered by topic.

Parameters:
- start_date (string, REQUIRED): Start of date range (ISO date)
- end_date (string, REQUIRED): End of date range (ISO date)
- topic (string): Filter by topic
- limit (number): Max results to return

Example:
```json
{
  "start_date": "2026-01-01",
  "end_date": "2026-03-31",
  "topic": "machine learning"
}
```

## Postgres Analysis Tools

Enabled when `DATABASE_URL` is configured. Connects directly to Postgres via `pg` (independent of the main database client).

### pg_analyze

Run a comprehensive Postgres health analysis. Returns table stats, index health, vacuum status, connection info, bloat estimates, Textrawl-specific checks (HNSW indexes, FTS indexes, orphaned chunks, RLS), and actionable recommendations.

Parameters:
- save (boolean): Save report to history directory for future comparison, default false

### pg_recommendations

Get actionable optimization recommendations for Postgres. Runs analysis and returns only recommendations.

Parameters:
- severity (enum): Filter by minimum severity — 'all' | 'critical' | 'warning' | 'info', default 'all'

### pg_report_history

View past Postgres analysis reports and compare trends over time.

Parameters:
- count (number): Number of past reports to load (1-50), default 5
- diff (boolean): Compare latest two reports and show changes, default true

## Environment Variables

Required (for database-backed features):
- DATABASE_URL: Postgres connection string (Neon pooled URL recommended; any pgvector-enabled Postgres works). Also powers the pg_analyze tools.

Conditional:
- OPENAI_API_KEY: For cloud embeddings (text-embedding-3-small, 1536 dims)
- GOOGLE_AI_API_KEY: For Google AI embeddings (gemini-embedding-2-preview, 3072 dims)
- OLLAMA_BASE_URL + OLLAMA_MODEL: For local embeddings (nomic-embed-text 1024d, nomic-embed-text-v2-moe 768d)

Optional:
- PG_REPORT_DIR: Directory for Postgres analysis report files (default ./reports/pg-analysis)
- API_BEARER_TOKEN: Auth token (min 32 chars, required for production)
- PORT: Server port (default 3000)
- LOG_LEVEL: debug/info/warn/error
- ALLOWED_ORIGINS: CORS whitelist
- ENABLE_MEMORY: Enable memory tools (default true)
- ENABLE_CONVERSATIONS: Enable conversation memory tools (default true)
- ENABLE_INSIGHTS: Enable proactive insight tools (default true)
- ENABLE_MEMORY_EXTRACTION: Enable LLM-based memory extraction (default false)
- ANTHROPIC_API_KEY: Required for extract_memories tool
- EXTRACTION_MODEL: Model for extraction (default claude-haiku-4-5-20251001)
- INSIGHT_MODEL: Model for insight synthesis (default claude-sonnet-4-6-20250514)

## CLI Tools

### Convert Files

```bash
pnpm convert -- mbox ~/Mail/archive.mbox
pnpm convert -- html ./saved-pages/ -r
pnpm convert -- takeout ./Takeout.zip
```

### Upload to Knowledge Base

```bash
pnpm upload -- ./converted/
pnpm upload -- ./converted/ --force  # Re-upload
```

## Architecture

### Hybrid Search

Textrawl combines:
1. Full-Text Search: PostgreSQL tsvector for exact keywords
2. Semantic Search: pgvector for meaning-based matching

Results merged using Reciprocal Rank Fusion (RRF).

### Chunking

- Max chunk: 512 tokens (~2048 chars)
- Overlap: 50 tokens
- Strategy: Paragraph-aware splitting

### Database Schema

Document tables (setup-db.sql):
- documents: Full documents with tsvector
- chunks: Document chunks with vector embeddings
- hybrid_search(): RPC for RRF fusion

Memory tables (setup-db-memory.sql):
- memory_entities: Named entities (people, concepts, projects)
- memory_observations: Atomic facts about entities with embeddings
- memory_relations: Directed relationships between entities
- memory_hybrid_search() / memory_semantic_search(): Memory search RPCs

## Deployment

### Docker

```bash
docker compose up -d
```

### Cloud Run

```bash
export GCP_PROJECT_ID=your-project
./scripts/deploy.sh
```

## Security

- API_BEARER_TOKEN required for production
- RLS enabled on all tables
- Health endpoints unprotected for load balancers

## Source Types

- note: Created via add_note tool
- file: Uploaded via CLI (PDF, DOCX, MBOX, EML, HTML)
- url: Web content (future)

## Agent Conventions

See /AGENTS.md for:
- Tool selection guide
- Error handling patterns
- Testing procedures
- Integration examples

## Documentation

- /AGENTS.md: Agent conventions
- /CLAUDE.md: Development instructions
- /docs/cli/: CLI tool documentation
- /docs/guides/security-hardening.mdx: Row Level Security
- /.well-known/mcp.json: MCP capability advertisement

## Support

- GitHub: https://github.com/jeffgreendesign/textrawl
- Issues: https://github.com/jeffgreendesign/textrawl/issues
