name: "Textrawl CodeQL Configuration"

# Extend the default security queries
queries:
  - uses: security-extended
  - uses: security-and-quality

# Local model pack with custom sanitizer annotations
packs:
  - .github/codeql/extensions
